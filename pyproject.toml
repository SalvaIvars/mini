[project]
name = "mini"
version = "0.2.0"
requires-python = ">=3.11"
dependencies = [
    "openai>=1.0.0",
    "python-dotenv>=1.0",
    "tiktoken>=0.7.0",
    "prompt-toolkit>=3.0.0",
    "rich>=13.0.0",
]

[project.scripts]
mini = "mini.cli:app"
